# 部署与配置指南

> 🛠️ TGAPI系统技术架构、部署方案和配置说明

## 📋 目录

1. [系统架构](#系统架构)
2. [环境要求](#环境要求)
3. [安装部署](#安装部署)
4. [配置说明](#配置说明)
5. [Bot Pool负载均衡](#bot-pool负载均衡)
6. [性能优化](#性能优化)
7. [监控运维](#监控运维)
8. [常见问题](#常见问题)

---

## 系统架构

### 整体架构图

```
┌─────────────────────────────────────────────────────────┐
│                     用户层                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │ Telegram用户 │  │  Web管理员   │  │  API调用方   │ │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘ │
└─────────┼──────────────────┼──────────────────┼─────────┘
          │                  │                  │
┌─────────┼──────────────────┼──────────────────┼─────────┐
│         │       服务层      │                  │         │
│  ┌──────▼──────┐    ┌─────▼──────┐    ┌──────▼──────┐ │
│  │  Bot Pool   │    │ FastAPI Web │    │  TGAPI接码  │ │
│  │ (多机器人)   │    │   管理后台   │    │    服务     │ │
│  │ - Bot 1     │    │ - Dashboard │    │ - 验证码推送 │ │
│  │ - Bot 2     │    │ - 用户管理   │    │ - API接口   │ │
│  │ - Bot N     │    │ - 统计报表   │    │ - 会话管理   │ │
│  └──────┬──────┘    └─────┬──────┘    └──────┬──────┘ │
└─────────┼──────────────────┼──────────────────┼─────────┘
          │                  │                  │
┌─────────┼──────────────────┼──────────────────┼─────────┐
│         │      数据层       │                  │         │
│  ┌──────▼──────────────────▼──────────────────▼──────┐ │
│  │              MySQL 8.0+ 数据库                     │ │
│  │  - bot_configs          - authorized_users        │ │
│  │  - bot_stats            - accounts                │ │
│  │  - tgapi_sessions       - tenants                 │ │
│  └─────────────────────────────────────────────────────┘ │
│  ┌─────────────────────────────────────────────────────┐ │
│  │              Redis 7.0+ 缓存                        │ │
│  │  - bot负载缓存          - 用户会话缓存              │ │
│  │  - bot心跳监控          - 消息计数缓存              │ │
│  └─────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

### 核心组件

**1. Bot Pool负载均衡系统**
- `BotPoolManager`: Bot池管理器，负责初始化和管理所有bot实例
- `LoadBalancer`: 负载均衡器，支持3种分配策略
- `HealthChecker`: 健康检查器，30秒心跳监控
- `BotInstance`: 单个bot实例封装，追踪负载和性能
- `BotPoolRedisCache`: Redis缓存层，优化性能

**2. Web管理后台**
- FastAPI框架，异步高性能
- Jinja2模板引擎，服务端渲染
- Bootstrap 5 + Chart.js前端
- JWT/Cookie会话管理
- Excel数据导出（openpyxl）

**3. TGAPI接码服务**
- Telethon客户端库
- 实时验证码监听
- RESTful API接口
- Token认证机制

**4. 账号管理模块**
- 批量导入/导出
- 格式转换（13种格式）
- 状态监控
- 租户隔离

---

## 环境要求

### 硬件要求

**最小配置（个人使用/测试）**
- CPU: 2核
- 内存: 4GB
- 硬盘: 20GB SSD
- 网络: 10Mbps

**推荐配置（商业运营）**
- CPU: 4核+
- 内存: 8GB+
- 硬盘: 50GB SSD+
- 网络: 100Mbps+

**Bot Pool大规模部署**
- CPU: 8核+ (支持多bot并发)
- 内存: 16GB+ (Redis缓存 + 多bot实例)
- 硬盘: 100GB SSD+
- 网络: 1Gbps+

### 软件要求

| 组件 | 版本要求 | 用途 | 说明 |
|-----|---------|------|------|
| Python | 3.9+ | 运行环境 | 必需 |
| MySQL | 8.0+ | 数据库 | 生产环境推荐 |
| SQLite | 3.31+ | 数据库 | 开发环境可选 |
| Redis | 7.0+ | 缓存/会话 | Bot Pool模式必需 |
| Node.js + PM2 | 14+ | 进程管理 | 生产环境推荐 |
| Nginx | 1.18+ | 反向代理 | 可选 |
| Docker | 20.10+ | 容器化部署 | 可选 |

**💡 数据库选择**：
- **MySQL 8.0+**（推荐）：生产环境、高并发、支持分布式、JSON支持
- **SQLite**：开发测试、个人使用、中小规模（< 10万用户）
- 支持运行时切换：修改 `.env` 中的 `DATABASE_TYPE`
- MySQL优势：连接池、索引优化、分区表、存储过程、视图

### 操作系统

支持以下操作系统：
- ✅ Ubuntu 20.04/22.04 LTS（推荐）
- ✅ Debian 11/12
- ✅ CentOS 8/Rocky Linux 8+
- ✅ macOS 11+（开发环境）
- ✅ Windows 10/11 + WSL2（开发环境）

---

## 安装部署

### 方式一：传统部署（推荐生产环境）

#### 步骤1: 安装系统依赖

**Ubuntu/Debian**
```bash
# 更新系统
sudo apt update && sudo apt upgrade -y

# 安装Python 3.9+
sudo apt install python3 python3-pip python3-venv -y

# 安装MySQL 8.0
sudo apt install mysql-server -y
sudo mysql_secure_installation

# 安装Redis (Bot Pool模式需要)
sudo apt install redis-server -y
sudo systemctl enable redis-server
sudo systemctl start redis-server

# 安装Node.js和PM2 (可选，用于进程管理)
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs
sudo npm install -g pm2

# 安装其他工具
sudo apt install git curl wget -y
```

**CentOS/Rocky Linux**
```bash
# 更新系统
sudo dnf update -y

# 安装Python 3.9+
sudo dnf install python39 python39-pip -y

# 安装MySQL 8.0
sudo dnf install mysql-server -y
sudo systemctl enable mysqld
sudo systemctl start mysqld
sudo mysql_secure_installation

# 安装Redis (Bot Pool模式需要)
sudo dnf install redis -y
sudo systemctl enable redis
sudo systemctl start redis

# 安装Node.js和PM2 (可选，用于进程管理)
curl -fsSL https://rpm.nodesource.com/setup_18.x | sudo bash -
sudo dnf install -y nodejs
sudo npm install -g pm2

# 安装其他工具
sudo dnf install git curl wget -y
```

#### 步骤2: 克隆项目

```bash
# 克隆仓库
git clone https://github.com/yourusername/TGAPI.git
cd TGAPI

# 创建Python虚拟环境
python3 -m venv venv
source venv/bin/activate  # Linux/macOS
# venv\Scripts\activate   # Windows

# 安装依赖
pip install -r requirements.txt
```

#### 步骤3: 配置MySQL数据库

```bash
# 登录MySQL
mysql -u root -p

# 创建数据库
CREATE DATABASE tgbot CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 创建用户并授权
CREATE USER 'tgbot'@'localhost' IDENTIFIED BY 'your_strong_password';
GRANT ALL PRIVILEGES ON tgbot.* TO 'tgbot'@'localhost';
FLUSH PRIVILEGES;
EXIT;
```

**初始化数据库表结构**：
```bash
# 方式1：使用start.sh初始化（推荐）
./start.sh init

# 方式2：手动初始化
python -c "from shared.database.init import db; db.initialize()"

# 方式3：直接导入SQL文件
mysql -u tgbot -p tgbot < shared/database/mysql_init.sql
mysql -u tgbot -p tgbot < shared/database/bot_pool_schema.sql
mysql -u tgbot -p tgbot < shared/database/authorized_users_schema_mysql.sql
```

**使用SQLite（开发/测试环境）**：
```bash
# 修改 .env
DATABASE_TYPE=sqlite
DATABASE_PATH=data/bot.db

# 初始化
./start.sh init
```

#### 步骤4: 配置环境变量

```bash
# 复制配置模板
cp .env.example .env

# 编辑配置文件
nano .env
```

**必填配置**（`.env`文件内容）：
```bash
# ==================== Telegram Bot配置 ====================
BOT_TOKEN=your_bot_token_here              # 从 @BotFather 获取
API_ID=your_api_id_here                     # 从 my.telegram.org 获取
API_HASH=your_api_hash_here                 # 从 my.telegram.org 获取

# ==================== 数据库配置 ====================
# 数据库类型: mysql 或 sqlite
DATABASE_TYPE=mysql

# MySQL配置（生产环境推荐）
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=tgbot
MYSQL_PASSWORD=your_strong_password
MYSQL_DATABASE=tgbot
MYSQL_CHARSET=utf8mb4

# SQLite配置（开发/测试环境）
# DATABASE_TYPE=sqlite
# DATABASE_PATH=data/bot.db

# ==================== Redis配置 ====================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=                             # 可选，如Redis设置了密码

# ==================== Web管理后台配置 ====================
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_admin_password          # 至少8位
API_BASE_URL=http://localhost:8000          # 修改为实际域名
SECRET_KEY=your_random_secret_key_here      # 随机生成，至少32位

# ==================== Bot Pool配置 ====================
ENABLE_BOT_POOL=false                       # true启用Bot Pool模式
BOT_POOL_STRATEGY=least_connections         # 负载均衡策略
HEALTH_CHECK_INTERVAL=30                    # 健康检查间隔（秒）
HEARTBEAT_TIMEOUT=60                        # 心跳超时时间（秒）

# ==================== 日志配置 ====================
LOG_LEVEL=INFO                              # DEBUG/INFO/WARNING/ERROR
LOG_FILE=./logs/tgapi.log

# ==================== 安全配置 ====================
MAX_LOGIN_ATTEMPTS=5                        # 最大登录尝试次数
LOGIN_LOCKOUT_MINUTES=15                    # 登录锁定时间（分钟）
SESSION_EXPIRE_HOURS=24                     # 会话过期时间（小时）
```

**生成随机SECRET_KEY**：
```bash
python3 -c "import secrets; print(secrets.token_urlsafe(32))"
```

#### 步骤5: 启动服务

**单Bot模式（传统模式）**
```bash
# 终端1: 启动Telegram Bot
python -m bot.main

# 终端2: 启动Web API服务
python -m api.server
```

**Bot Pool模式（高可用）**
```bash
# 1. 确保Redis正在运行
redis-cli ping  # 应返回 PONG

# 2. 修改.env配置
ENABLE_BOT_POOL=true

# 3. 启动服务
# 终端1: 启动Bot Pool
python -m bot.main

# 终端2: 启动Web API服务
python -m api.server
```

#### 步骤6: 验证部署

```bash
# 检查Bot是否启动
# 在Telegram中向bot发送 /start

# 检查Web服务
curl http://localhost:8000/health
# 应返回: {"status":"ok"}

# 访问管理后台
# 浏览器打开: http://localhost:8000/admin
# 使用.env中配置的账号密码登录
```

---

### 方式二：Docker部署（快速启动）

#### 创建docker-compose.yml

```yaml
version: '3.8'

services:
  # Redis服务（Bot Pool模式需要）
  redis:
    image: redis:7-alpine
    container_name: tgapi_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    networks:
      - tgapi_network
    restart: unless-stopped

  # Bot服务
  bot:
    build: .
    container_name: tgapi_bot
    command: python -m bot.main
    env_file:
      - .env
    depends_on:
      - redis
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data       # SQLite数据库文件挂载
      - ./sessions:/app/sessions
    networks:
      - tgapi_network
    restart: unless-stopped

  api:
    build: .
    container_name: tgapi_api
    command: python -m api.server
    env_file:
      - .env
    ports:
      - "8000:8000"
    depends_on:
      - mysql
      - redis
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data
    networks:
      - tgapi_network
    restart: unless-stopped

volumes:
  mysql_data:
  redis_data:

networks:
  tgapi_network:
    driver: bridge
```

#### 启动Docker服务

```bash
# 构建并启动所有服务
docker-compose up -d

# 查看运行状态
docker-compose ps

# 查看日志
docker-compose logs -f bot
docker-compose logs -f api

# 停止服务
docker-compose down
```

---

## 配置说明

### Bot配置详解

#### 1. 单Bot模式配置

最简配置：
```bash
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz123456
API_ID=12345678
API_HASH=abcdef1234567890abcdef1234567890
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_password
```

#### 2. Bot Pool模式配置

启用Bot Pool需要额外配置：
```bash
# 启用Bot Pool
ENABLE_BOT_POOL=true

# Redis连接
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# 负载均衡策略（3选1）
BOT_POOL_STRATEGY=least_connections    # 最少连接（推荐）
# BOT_POOL_STRATEGY=weighted_random    # 加权随机
# BOT_POOL_STRATEGY=priority_based     # 优先级

# 健康监控
HEALTH_CHECK_INTERVAL=30               # 检查间隔30秒
HEARTBEAT_TIMEOUT=60                   # 心跳超时60秒
```

**负载均衡策略对比**

| 策略 | 原理 | 适用场景 | 优点 | 缺点 |
|-----|------|---------|------|------|
| least_connections | 分配给负载最低的bot | 通用场景 | 负载均衡效果最好 | 需频繁查询负载 |
| weighted_random | 按优先级加权随机 | bot性能差异大 | 灵活性高 | 可能不够均衡 |
| priority_based | 按优先级顺序选择 | 主备模式 | 简单可靠 | 主bot压力大 |

#### 3. 数据库配置

**MySQL配置（生产环境推荐）**
```bash
DATABASE_TYPE=mysql
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=tgbot
MYSQL_PASSWORD=your_strong_password
MYSQL_DATABASE=tgbot
MYSQL_CHARSET=utf8mb4
```

**SQLite配置（开发/测试环境）**
```bash
DATABASE_TYPE=sqlite
DATABASE_PATH=data/bot.db
```

**💡 选择建议**：
- **MySQL 8.0+**：生产环境、大规模部署、高并发场景
- **SQLite**：开发测试、个人使用、中小规模（< 10万用户）
- 支持运行时切换：修改 `DATABASE_TYPE` 并重新初始化

#### 4. 安全配置

```bash
# 会话管理
SECRET_KEY=your_random_secret_key_32_chars_minimum
SESSION_EXPIRE_HOURS=24

# 登录保护
MAX_LOGIN_ATTEMPTS=5
LOGIN_LOCKOUT_MINUTES=15

# CORS配置（如需跨域）
ALLOWED_ORIGINS=https://yourdomain.com,https://admin.yourdomain.com

# HTTPS配置（生产环境必须）
FORCE_HTTPS=true
```

---

## Bot Pool负载均衡

### 架构说明

Bot Pool系统由5个核心组件组成：

```
┌─────────────────────────────────────────────┐
│         BotPoolManager（池管理器）            │
│  - 初始化bot池                               │
│  - 动态添加/删除bot                          │
│  - 统一管理所有bot实例                        │
└───────────────┬─────────────────────────────┘
                │
        ┌───────┴──────────┐
        │                  │
┌───────▼──────┐   ┌───────▼──────┐
│LoadBalancer  │   │HealthChecker │
│ - 用户分配    │   │ - 30s心跳    │
│ - 会话保持    │   │ - 故障隔离    │
└───────┬──────┘   └───────┬──────┘
        │                  │
        └─────────┬────────┘
                  │
        ┌─────────▼──────────┐
        │ BotPoolRedisCache │
        │  - 负载缓存         │
        │  - 会话缓存         │
        │  - 心跳缓存         │
        └────────────────────┘
```

### 部署Bot Pool

#### 步骤1: 准备多个Bot Token

1. 找 @BotFather 创建3个bot（或更多）
2. 获取每个bot的Token
3. 记录bot用户名

示例：
```
Bot 1: @MyMainBot      Token: 123456:ABC...
Bot 2: @MyBackupBot1   Token: 234567:DEF...
Bot 3: @MyBackupBot2   Token: 345678:GHI...
```

#### 步骤2: 配置环境变量

```bash
# 启用Bot Pool
ENABLE_BOT_POOL=true

# Redis必须启用
REDIS_HOST=localhost
REDIS_PORT=6379

# 选择负载均衡策略
BOT_POOL_STRATEGY=least_connections
```

#### 步骤3: 添加Bot到池中

**方式1: Web管理后台添加（推荐）**
```
1. 访问 http://localhost:8000/admin/bot-pool
2. 点击"添加Bot"按钮
3. 填写信息：
   - Bot Token: 123456:ABC...
   - Bot用户名: @MyMainBot
   - Bot名称: 主要Bot
   - 最大负载: 100（同时活跃用户数）
   - 优先级: 10（1-10，数字越大越优先）
   - 描述: 主力机器人
4. 点击"添加"
```

**方式2: 直接写入数据库**
```sql
INSERT INTO bot_configs (
    bot_token,
    bot_username,
    bot_name,
    max_load,
    priority,
    description,
    status
) VALUES
('123456:ABC...', '@MyMainBot', '主要Bot', 100, 10, '主力机器人', 'active'),
('234567:DEF...', '@MyBackupBot1', '备用Bot1', 100, 5, '备用机器人1', 'active'),
('345678:GHI...', '@MyBackupBot2', '备用Bot2', 100, 5, '备用机器人2', 'active');
```

#### 步骤4: 启动Bot Pool

```bash
# 启动
python -m bot.main

# 日志应显示：
# 🚀 启动模式: Bot Pool (负载均衡)
# 📦 从数据库加载到 3 个bot配置
# ✅ Bot @MyMainBot 启动成功
# ✅ Bot @MyBackupBot1 启动成功
# ✅ Bot @MyBackupBot2 启动成功
# ✅ Bot Pool运行中...
```

### 监控Bot Pool

#### 1. Web仪表板监控

访问：`http://localhost:8000/admin/bot-pool`

显示内容：
- 📊 总bot数量、活跃bot数、总活跃用户数
- 📈 每个bot的实时负载条（绿色/黄色/红色）
- 🔄 Bot状态（active/inactive/error/maintenance）
- ⚡ 性能指标（CPU、内存、每分钟消息数）
- 📝 最近事件日志

#### 2. 命令行监控

```bash
# 查看Redis中的负载数据
redis-cli

# 查看所有bot负载
ZRANGE bot_pool:available_bots 0 -1 WITHSCORES

# 查看特定bot心跳
GET bot_pool:bot:heartbeat:1

# 查看用户会话映射
GET bot_pool:user:session:123456789
```

#### 3. 数据库查询

```sql
-- 查看所有bot状态
SELECT
    bc.id,
    bc.bot_username,
    bc.status,
    bs.active_users,
    bs.messages_per_min,
    bs.last_heartbeat
FROM bot_configs bc
LEFT JOIN bot_stats bs ON bc.id = bs.bot_id
ORDER BY bc.priority DESC;

-- 查看负载历史趋势
SELECT
    bot_id,
    AVG(active_users) as avg_users,
    MAX(active_users) as peak_users,
    DATE(timestamp) as date
FROM bot_load_history
WHERE timestamp >= DATE_SUB(NOW(), INTERVAL 7 DAY)
GROUP BY bot_id, DATE(timestamp)
ORDER BY date DESC;
```

### 故障处理

**场景1: 单个bot崩溃**
- HealthChecker检测到心跳超时（60秒）
- 自动标记bot为`error`状态
- LoadBalancer停止分配新用户到该bot
- 已分配用户会话保持不变（直到手动重新分配）

**场景2: 手动维护bot**
```sql
-- 设置为维护模式
UPDATE bot_configs SET status='maintenance' WHERE id=1;

-- 恢复正常
UPDATE bot_configs SET status='active' WHERE id=1;
```

**场景3: 动态扩容**
- 访问Web管理后台 Bot Pool页面
- 点击"添加Bot"
- 填写新bot信息并提交
- 系统自动启动新bot（无需重启服务）

---

## 性能优化

### Redis缓存优化

**缓存策略**
- Bot状态缓存：60秒TTL
- Bot负载缓存：30秒TTL
- 用户会话缓存：30分钟TTL
- Bot心跳缓存：10秒TTL

**缓存命中率监控**
```bash
redis-cli INFO stats | grep keyspace
# 目标：缓存命中率 > 90%
```

**优化Redis配置**
```bash
# /etc/redis/redis.conf

# 最大内存（根据服务器配置调整）
maxmemory 2gb

# 内存淘汰策略
maxmemory-policy allkeys-lru

# 持久化（根据需求选择）
appendonly yes
appendfsync everysec
```

### MySQL优化

**索引优化**
```sql
-- bot_stats表索引
CREATE INDEX idx_bot_id ON bot_stats(bot_id);
CREATE INDEX idx_last_heartbeat ON bot_stats(last_heartbeat);

-- user_bot_sessions表索引
CREATE INDEX idx_telegram_id ON user_bot_sessions(telegram_id);
CREATE INDEX idx_bot_id ON user_bot_sessions(assigned_bot_id);
CREATE INDEX idx_last_interaction ON user_bot_sessions(last_interaction);

-- bot_events表索引
CREATE INDEX idx_bot_event ON bot_events(bot_id, event_type);
CREATE INDEX idx_created_at ON bot_events(created_at);

-- 查看索引使用情况
SHOW INDEX FROM bot_stats;
```

**配置优化 (my.cnf)**
```ini
[mysqld]
# InnoDB配置
innodb_buffer_pool_size = 4G           # 缓冲池大小（物理内存的50-70%）
innodb_log_file_size = 256M            # 日志文件大小
innodb_flush_log_at_trx_commit = 2     # 日志刷新策略（性能优化）
innodb_flush_method = O_DIRECT         # 跳过系统缓存

# 连接配置
max_connections = 500                   # 最大连接数
max_connect_errors = 1000              # 最大连接错误数

# 查询缓存（MySQL 8.0已移除）
# query_cache_size = 0

# 字符集
character_set_server = utf8mb4
collation_server = utf8mb4_unicode_ci

# 慢查询日志
slow_query_log = 1
slow_query_log_file = /var/log/mysql/slow.log
long_query_time = 2                     # 慢查询阈值（秒）
```

**定期维护**
```bash
# 优化表
mysqlcheck -u tgbot -p --optimize tgbot

# 分析表
mysqlcheck -u tgbot -p --analyze tgbot

# 修复表
mysqlcheck -u tgbot -p --repair tgbot
```

### Bot Pool优化

**负载均衡参数调优**
```bash
# 健康检查间隔（秒）
# 值越小越灵敏，但消耗更多资源
HEALTH_CHECK_INTERVAL=30        # 默认30秒，可调整为15-60秒

# 心跳超时时间（秒）
# 超过此时间未收到心跳则标记bot为故障
HEARTBEAT_TIMEOUT=60            # 默认60秒，可调整为30-120秒

# 会话保持时间（秒）
# Redis中用户会话缓存TTL
USER_SESSION_TTL=1800           # 默认30分钟，可调整为15-60分钟
```

**Bot实例资源限制**
```python
# bot/pool/bot_instance.py
class BotInstance:
    def __init__(self, ..., max_load=100):
        self.max_load = max_load  # 根据服务器配置调整
        # 2核4GB服务器: max_load=50
        # 4核8GB服务器: max_load=100
        # 8核16GB服务器: max_load=200
```

---

## 监控运维

### 日志管理

**日志配置**
```bash
# .env
LOG_LEVEL=INFO                          # DEBUG/INFO/WARNING/ERROR
LOG_FILE=./logs/tgapi.log
LOG_MAX_SIZE=100                        # MB
LOG_BACKUP_COUNT=7                      # 保留7天
```

**日志查看**
```bash
# 实时查看bot日志
tail -f logs/tgapi.log

# 查看错误日志
grep ERROR logs/tgapi.log

# 查看特定bot日志
grep "bot_username=@MyMainBot" logs/tgapi.log
```

### 系统监控

**进程监控**
```bash
# 查看Python进程
ps aux | grep python

# 查看资源占用
top -p $(pgrep -f "bot.main")
```

**使用systemd管理服务**

创建服务文件 `/etc/systemd/system/tgapi-bot.service`：
```ini
[Unit]
Description=TGAPI Bot Service
After=network.target mysql.service redis.service

[Service]
Type=simple
User=tgapi
WorkingDirectory=/opt/TGAPI
Environment="PATH=/opt/TGAPI/venv/bin"
ExecStart=/opt/TGAPI/venv/bin/python -m bot.main
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

创建API服务文件 `/etc/systemd/system/tgapi-api.service`：
```ini
[Unit]
Description=TGAPI API Service
After=network.target mysql.service redis.service

[Service]
Type=simple
User=tgapi
WorkingDirectory=/opt/TGAPI
Environment="PATH=/opt/TGAPI/venv/bin"
ExecStart=/opt/TGAPI/venv/bin/python -m api.server
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

**启用服务**
```bash
# 重载systemd配置
sudo systemctl daemon-reload

# 启动服务
sudo systemctl start tgapi-bot
sudo systemctl start tgapi-api

# 设置开机自启
sudo systemctl enable tgapi-bot
sudo systemctl enable tgapi-api

# 查看状态
sudo systemctl status tgapi-bot
sudo systemctl status tgapi-api

# 查看日志
sudo journalctl -u tgapi-bot -f
```

### 备份策略

**MySQL数据库备份**
```bash
#!/bin/bash
# backup.sh

BACKUP_DIR="/backup/tgapi"
DATE=$(date +%Y%m%d_%H%M%S)

# MySQL备份
mysqldump -u tgbot -p'your_password' \
  --single-transaction \
  --routines \
  --triggers \
  --events \
  tgbot > $BACKUP_DIR/tgbot_$DATE.sql

# 压缩备份
gzip $BACKUP_DIR/tgbot_$DATE.sql

# 删除7天前的备份
find $BACKUP_DIR -name "tgbot_*.sql.gz" -mtime +7 -delete

# 备份日志文件（可选）
tar -czf $BACKUP_DIR/logs_$DATE.tar.gz logs/
```

**SQLite备份（如使用SQLite）**
```bash
# 方式1：使用start.sh备份（推荐）
./start.sh db backup

# 方式2：手动备份
cp data/bot.db data/backups/bot_$(date +%Y%m%d_%H%M%S).db
```

**添加到crontab**
```bash
# 每天凌晨3点自动备份
0 3 * * * /opt/TGAPI/backup.sh

# 或使用start.sh
0 3 * * * cd /opt/TGAPI && ./start.sh db backup
```

**恢复数据库**
```bash
# MySQL恢复
mysql -u tgbot -p tgbot < backup.sql

# SQLite恢复
cp backup.db data/bot.db
```

---

## 常见问题

### Q1: Bot Pool启动失败，显示Redis连接错误

**原因**：Redis未启动或连接配置错误

**解决**：
```bash
# 检查Redis状态
systemctl status redis

# 启动Redis
systemctl start redis

# 测试连接
redis-cli ping  # 应返回 PONG

# 检查.env配置
REDIS_HOST=localhost
REDIS_PORT=6379
```

### Q2: MySQL连接错误

**错误信息**：`Can't connect to MySQL server` 或 `Access denied`

**原因**：MySQL服务未启动或连接配置错误

**解决**：
```bash
# 1. 检查MySQL服务状态
systemctl status mysql

# 2. 启动MySQL
systemctl start mysql

# 3. 测试连接
mysql -u tgbot -p

# 4. 检查.env配置
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=tgbot
MYSQL_PASSWORD=your_password

# 5. 重新授权用户
mysql -u root -p
GRANT ALL PRIVILEGES ON tgbot.* TO 'tgbot'@'localhost';
FLUSH PRIVILEGES;
```

### Q3: Bot心跳检查失败

**原因**：网络波动或bot实例卡死

**解决**：
```bash
# 1. 检查bot进程
ps aux | grep bot.main

# 2. 查看bot日志
tail -f logs/tgapi.log | grep ERROR

# 3. 重启特定bot（Web管理后台）
# 进入Bot Pool页面 -> 点击重启按钮

# 4. 调整心跳参数（降低敏感度）
HEARTBEAT_TIMEOUT=120  # 增加到2分钟
```

### Q4: 负载分配不均衡

**现象**：某个bot负载很高，其他bot空闲

**原因**：会话保持导致用户固定在同一bot

**解决**：
```sql
-- 手动清除会话缓存（用户会重新分配）
redis-cli FLUSHDB

-- 或调整会话TTL（在.env中）
USER_SESSION_TTL=900  # 减少到15分钟
```

### Q5: Web管理后台无法访问

**原因**：端口被占用或防火墙阻止

**解决**：
```bash
# 检查端口占用
sudo netstat -tlnp | grep 8000

# 检查防火墙
sudo ufw status
sudo ufw allow 8000/tcp

# 修改监听地址（.env）
API_HOST=0.0.0.0  # 监听所有网卡
API_PORT=8000
```

### Q6: TGAPI验证码无法接收

**原因**：Session失效或账号被封禁

**解决**：
```bash
# 1. 检查Session有效性
# 在bot中发送测试命令，查看错误日志

# 2. 检查账号状态
# 使用 /check_account 命令检查

# 3. 重新生成Session
# 使用新的Session字符串创建TGAPI链接

# 4. 查看TGAPI日志
grep "tgapi_token=YOUR_TOKEN" logs/tgapi.log
```

### Q7: MySQL占用内存过高

**原因**：InnoDB缓冲池配置不当或连接数过多

**解决**：
```bash
# 1. 查看MySQL内存使用
mysql -u root -p -e "SHOW ENGINE INNODB STATUS\G" | grep -A 20 "BUFFER POOL"

# 2. 调整配置 /etc/mysql/my.cnf
[mysqld]
innodb_buffer_pool_size = 2G           # 调整为物理内存的50-70%
max_connections = 200                  # 限制最大连接数

# 3. 重启MySQL
systemctl restart mysql

# 4. 查看当前连接数
mysql -u root -p -e "SHOW STATUS LIKE 'Threads_connected';"

# 5. 清理过期数据（减少表大小）
mysql -u tgbot -p tgbot -e "DELETE FROM code_push_logs WHERE push_time < DATE_SUB(NOW(), INTERVAL 30 DAY);"
mysql -u tgbot -p tgbot -e "DELETE FROM operation_logs WHERE created_at < DATE_SUB(NOW(), INTERVAL 90 DAY);"
mysql -u tgbot -p tgbot -e "OPTIMIZE TABLE code_push_logs, operation_logs;"
```

### Q8: 部署后无法发送消息给bot

**原因**：bot未正确设置Webhook或Polling

**解决**：
```bash
# 检查bot模式（本项目使用Polling）
# 确保没有设置Webhook

# 清除Webhook（如果有）
curl -X POST https://api.telegram.org/bot<YOUR_TOKEN>/deleteWebhook

# 重启bot
systemctl restart tgapi-bot

# 查看日志确认启动成功
journalctl -u tgapi-bot -f
```

---

## 生产环境检查清单

部署到生产环境前，请确认以下各项：

### 安全检查
- [ ] 修改了所有默认密码（MySQL、Redis、管理员）
- [ ] SESSION_SECRET_KEY使用随机生成的32位以上字符串
- [ ] 启用了HTTPS（配置SSL证书）
- [ ] 配置了防火墙（仅开放必要端口：8000, 3306, 6379）
- [ ] 禁用了DEBUG模式（DEBUG=false）
- [ ] Redis设置了密码保护（如启用Bot Pool）
- [ ] MySQL仅允许本地连接（或使用防火墙限制）
- [ ] MySQL用户权限最小化（不使用root）

### 性能检查
- [ ] MySQL索引已创建（schema自动创建）
- [ ] MySQL配置已优化（innodb_buffer_pool_size等）
- [ ] 数据库连接池已配置
- [ ] Redis持久化已配置（AOF或RDB）
- [ ] Bot Pool负载参数已根据服务器配置调整
- [ ] 日志已启用每日切割和30天自动清理

### 监控检查
- [ ] 配置了systemd服务（开机自启）
- [ ] 设置了数据库定时备份
- [ ] 配置了日志监控（可选：ELK/Grafana）
- [ ] 设置了告警通知（可选：邮件/Telegram）

### 功能检查
- [ ] Bot能正常响应 /start 命令
- [ ] Web管理后台可以登录
- [ ] TGAPI链接可以正常创建和接收验证码
- [ ] Bot Pool多bot负载均衡正常工作
- [ ] 数据统计图表正常显示

---

**📌 提示**：生产环境部署建议使用Nginx反向代理+SSL，并配置CDN加速。详细配置可咨询管理员。
